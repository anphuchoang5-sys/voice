# 待处理问题记录

> 由 Claude 审查代码后记录，每次审查后更新状态。

---

## 阶段二遗留

### Q1 — 静音阈值可能在嘈杂环境失灵
- **文件**：`src/constants/config.ts`（已删除）

> 🔍 **这是什么**：App 靠检测音量判断用户是否说完了话，-50 在有噪音的环境里可能判断失误
> ⚠️ **风险**：比赛演示现场有环境噪音时可能当场翻车
> 🛠️ **修法**：N/A — 阶段六切换到 Android 原生 STT，静音检测由 Google ASR 系统层处理，`SILENCE_THRESHOLD_DB` 常量已删除

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| ✅ 自然消解 | 阶段六切换原生 STT，静音检测移交系统处理 | Claude |

---

### Q2 — 调试音量数值暴露在 UI 上
- **文件**：`app/record.tsx`（已重写）

> 🔍 **这是什么**：录音时界面实时显示麦克风 dB 数值，是调试用的
> ⚠️ **风险**：演示时显得未完成
> 🛠️ **修法**：N/A — 阶段六切换到原生 STT 后，record.tsx 完全重写，metering 数据来源（expo-av）已移除，dB 显示自然消失

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| ✅ 自然消解 | 阶段六重写 record.tsx，metering 数据来源已移除 | Claude |

---

## 阶段三遗留

### Q3 — EventConfirmCard 取消时无滑出动画
- **文件**：`src/components/EventConfirmCard.tsx`

> 🔍 **这是什么**：确认卡弹出有动画，但取消时直接消失
> ⚠️ **风险**：体验廉价
> 🛠️ **修法**：`isMounted` 状态控制，动画结束后再 unmount

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| ✅ 已解决 | 阶段四 `isMounted` + `runOnJS(finishUnmount)` 修复 | Codex |

---

## 阶段四新增

### Q4 — `getOrCreateVoiceCalendar()` 被多调用一次
- **文件**：`app/record.tsx`

> 🔍 **这是什么**：保存事件时同一个函数被调了两遍
> ⚠️ **风险**：功能正常但代码冗余
> 🛠️ **修法**：删除 `record.tsx` 里多余的那行调用

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| ✅ 已解决 | 阶段五前置修复中删除 | Codex |

---

### Q5 — 写入失败提示语误导用户
- **文件**：`app/record.tsx`

> 🔍 **这是什么**：权限已通过但写入失败时，提示"请检查权限"，让用户去白翻设置
> ⚠️ **风险**：用户困惑
> 🛠️ **修法**：改为"写入日历失败，请重试"

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| ✅ 已解决 | 阶段五前置修复中修改 | Codex |

---

### Q6 — 日期解析代码在多处重复
- **文件**：`calendar.service.ts` / `notification.service.ts` / `index.tsx` / `CalendarGrid.tsx`

> 🔍 **这是什么**：`parseDateString` 和 `formatDate` 函数在多个文件里各写了一份
> ⚠️ **风险**：改一处容易漏掉其他地方
> 🛠️ **修法**：提取到 `src/utils/date.ts` 统一管理

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| 🔴 未解决 | 阶段六统一整理 | — |

---

### Q7 — npm 日志文件反复泄漏进项目目录
- **文件**：`VoiceCalendar/%USERPROFILE%/...`

> 🔍 **这是什么**：Codex 跑 npm 时日志写到了项目目录里
> ⚠️ **风险**：会被 git 一起提交，污染代码库
> 🛠️ **修法**：删除目录 + `.gitignore` 加防护 + 阶段六加 `.npmrc` 根治

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| ⚠️ 临时处理 | Claude 每次审查时手动删除，已加 `.gitignore`，但根本原因未解决 | Claude |

---

## 阶段五新增

### Q8 — 事件详情页把系统内部 ID 显示给用户看
- **文件**：`app/event/[id].tsx` 第 155 行

> 🔍 **这是什么**：事件详情页有一行"系统 ID: xxxx"，显示的是 Android 日历给这个事件分配的内部编号，普通用户完全不需要看到这个
> ⚠️ **风险**：显得像开发者调试界面，评委一眼看出未完成，扣印象分
> 🛠️ **修法**：删除 `<DetailRow label="系统 ID" value={event.id} />` 这一行

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| 🔴 未解决 | 阶段六处理 | — |

---

### Q9 — 左滑删除没有确认步骤，容易误删
- **文件**：`src/components/EventListItem.tsx`

> 🔍 **这是什么**：在主页事件列表左滑后点删除，事件会立即消失，没有"确定要删除吗？"的二次确认。但在事件详情页删除是有确认弹窗的，两个地方行为不一致。
> ⚠️ **风险**：手滑误删事件，体验差；与详情页行为不统一，让用户困惑
> 🛠️ **修法**：在 `EventListItem` 的 `onDelete` 触发时，加 `Alert.alert` 二次确认，和详情页保持一致

| 是否已解决 | 解决方式 | 决策人 |
|-----------|---------|--------|
| 🔴 未解决 | 阶段六处理 | — |

---

## 汇总

| 编号 | 阶段 | 问题简述 | 是否已解决 | 优先级 |
|------|------|---------|-----------|--------|
| Q1 | 二 | 静音阈值偏严 | ✅ 自然消解 | — |
| Q2 | 二 | 调试音量显示在 UI | ✅ 自然消解 | — |
| Q3 | 三 | 确认卡无滑出动画 | ✅ 已解决 | — |
| Q4 | 四 | 重复调用日历初始化 | ✅ 已解决 | — |
| Q5 | 四 | 错误提示语误导用户 | ✅ 已解决 | — |
| Q6 | 四 | 日期解析代码重复 | 🔴 未解决 | 低 |
| Q7 | 四 | npm 日志泄漏进项目 | ⚠️ 临时处理 | 阶段六根治 |
| Q8 | 五 | 系统 ID 暴露给用户 | 🔴 未解决 | **高** |
| Q9 | 五 | 左滑删除无确认 | 🔴 未解决 | **高** |
